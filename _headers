  # Allow figma's SW to fetch payload.js cross-origin
  /payload.js
    Access-Control-Allow-Origin: *
    Access-Control-Allow-Methods: GET
    Cache-Control: no-store, no-cache

  # Per-path CORS removed — wildcard CORS on all paths is a security risk.
  # Add specific CORS headers to individual paths as needed.
